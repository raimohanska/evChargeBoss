{
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node12 --outfile=dist/bundle.cjs --inject:src/polyfill.ts",
    "test": "TZ=Europe/Helsinki CONFIG_FILE=test/fixtures/config.json node --experimental-strip-types --test test/planner.test.ts"
  },
  "dependencies": {
    "mqtt": "^4.3.8",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.13",
    "esbuild": "^0.28.0"
  }
}
