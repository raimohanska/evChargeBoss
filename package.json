{
  "type": "module",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node12 --outfile=dist/bundle.cjs --inject:polyfill.ts",
    "test": "TZ=Europe/Helsinki node --experimental-strip-types --test test/planner.test.ts"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.13",
    "esbuild": "^0.28.0"
  }
}
